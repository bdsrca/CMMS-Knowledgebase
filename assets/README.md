# Assets

This folder is reserved for sanitized diagrams, icons, or exported visuals that support the technical showcase.

The repository currently relies on Mermaid diagrams in `diagrams/` and the embedded diagram in `README.md`. Add image assets here only after verifying they do not contain private branding, production data, tenant IDs, user names, or internal URLs.
